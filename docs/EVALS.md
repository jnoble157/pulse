# Evals

The layered eval harness (`packages/evals`, golden corpus, Braintrust gates) lived in the **pre–ADR-038** tree. It was removed with the analytics surface.

If you bring extraction + evals back, recover `docs/EVALS.md` and `packages/evals` from git history and re-open an ADR.
