{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm turbo run build --filter=@pulse/web",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "bash -c 'if [ -n \"${VERCEL_FORCE_DEPLOY:-}\" ]; then exit 1; fi; if git diff --quiet HEAD^ HEAD -- apps/web packages; then exit 0; else exit 1; fi'"
}
